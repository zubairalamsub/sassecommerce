apiVersion: apps/v1
kind: Deployment
metadata: { name: promotion-service, namespace: ecommerce, labels: { app: promotion-service } }
spec:
  replicas: 2
  selector: { matchLabels: { app: promotion-service } }
  template:
    metadata: { labels: { app: promotion-service } }
    spec:
      containers:
        - name: promotion-service
          image: ghcr.io/saajan/promotion-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ name: http, containerPort: 8091 }]
          envFrom: [{ configMapRef: { name: shared-config } }]
          env:
            - { name: PORT, value: "8091" }
            - { name: DB_HOST, value: postgres }
            - { name: DB_NAME, value: promotion_db }
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: username } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: password } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: jwt-secret, key: secret } }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 512Mi }
          livenessProbe:  { httpGet: { path: /health, port: http }, initialDelaySeconds: 30, periodSeconds: 30 }
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 5,  periodSeconds: 10 }
---
apiVersion: v1
kind: Service
metadata: { name: promotion-service, namespace: ecommerce }
spec:
  type: ClusterIP
  selector: { app: promotion-service }
  ports: [{ name: http, port: 8091, targetPort: http }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: promotion-service, namespace: ecommerce }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: promotion-service }
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
