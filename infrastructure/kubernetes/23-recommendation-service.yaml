apiVersion: apps/v1
kind: Deployment
metadata: { name: recommendation-service, namespace: ecommerce, labels: { app: recommendation-service } }
spec:
  replicas: 2
  selector: { matchLabels: { app: recommendation-service } }
  template:
    metadata: { labels: { app: recommendation-service } }
    spec:
      containers:
        - name: recommendation-service
          image: ghcr.io/saajan/recommendation-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ name: http, containerPort: 8094 }]
          envFrom: [{ configMapRef: { name: shared-config } }]
          env:
            - { name: PORT, value: "8094" }
            - { name: DB_HOST, value: postgres }
            - { name: DB_NAME, value: recommendation_db }
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: username } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: password } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: jwt-secret, key: secret } }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits:   { cpu: 1000m, memory: 1Gi }
          livenessProbe:  { httpGet: { path: /health, port: http }, initialDelaySeconds: 30, periodSeconds: 30 }
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 5,  periodSeconds: 10 }
---
apiVersion: v1
kind: Service
metadata: { name: recommendation-service, namespace: ecommerce }
spec:
  type: ClusterIP
  selector: { app: recommendation-service }
  ports: [{ name: http, port: 8094, targetPort: http }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: recommendation-service, namespace: ecommerce }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: recommendation-service }
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
