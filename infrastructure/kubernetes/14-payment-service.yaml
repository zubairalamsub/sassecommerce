apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: ecommerce
  labels: { app: payment-service }
spec:
  replicas: 2
  selector: { matchLabels: { app: payment-service } }
  template:
    metadata:
      labels: { app: payment-service }
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/saajan/payment-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 8085 }
          envFrom:
            - configMapRef: { name: shared-config }
          env:
            - { name: ASPNETCORE_URLS, value: http://+:8085 }
            - { name: ASPNETCORE_ENVIRONMENT, value: Production }
            - { name: ConnectionStrings__DefaultConnection, value: "Host=postgres;Port=5432;Database=payment_db;Username=$(DB_USER);Password=$(DB_PASSWORD)" }
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: username } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: password } }
            - name: Jwt__Secret
              valueFrom: { secretKeyRef: { name: jwt-secret, key: secret } }
            - name: SSLCommerz__StoreId
              valueFrom: { secretKeyRef: { name: payment-providers, key: sslcommerz-store-id } }
            - name: SSLCommerz__StorePassword
              valueFrom: { secretKeyRef: { name: payment-providers, key: sslcommerz-store-password } }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits:   { cpu: 1000m, memory: 1Gi }
          livenessProbe:  { httpGet: { path: /health, port: http }, initialDelaySeconds: 30, periodSeconds: 30 }
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 5,  periodSeconds: 10 }
---
apiVersion: v1
kind: Service
metadata: { name: payment-service, namespace: ecommerce }
spec:
  type: ClusterIP
  selector: { app: payment-service }
  ports:
    - { name: http, port: 8085, targetPort: http }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: payment-service, namespace: ecommerce }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: payment-service }
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
